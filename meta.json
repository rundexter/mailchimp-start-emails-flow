/*
 * Metadata describing the module operation
 */
{
    "inputs": [
        {
            "id": "workflow_id",
            "title":"Workflow ID"
        }
    ],
    "outputs": [],
    "providers": [{
        "name": "mailchimp "
    }],
    "icon": "dex-mailchimp",
    "title": "Mailchimp - Start Emails in Workflow"
}
